---

actor:
    name: test_camera
    host: localhost
    port: 9999
    log_dir: false
    default_cameras:
        - test_camera

cameras:
    test_camera:
        uid: DEV_12345
        autoconnect: true
        connection:
            device: /dev/camera
